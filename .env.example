# --- Core ---
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/world_cup
ADMIN_EMAILS=you@example.com,other@example.com

# --- Better Auth ---
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Auto result sync (cron) ---
# Shared secret the cron endpoint requires as `Authorization: Bearer <secret>`.
# Set the same value in Vercel and in your Upstash QStash schedule headers.
CRON_SECRET=

# Results feed: TheSportsDB (free). All optional — defaults work out of the box
# with the free public key. Only set THESPORTSDB_KEY if you have a Patreon key.
# THESPORTSDB_KEY=3
# THESPORTSDB_LEAGUE=4429   # FIFA World Cup
# THESPORTSDB_SEASON=2026
